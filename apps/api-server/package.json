{
  "name": "@talos/api-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@talos/orchestrator": "*",
    "@talos/agent-runtime": "*",
    "@talos/task-graph": "*",
    "@talos/workflow-engine": "*",
    "@talos/memory-engine": "*",
    "@talos/embeddings-service": "*",
    "@talos/workflow-db": "*",
    "@talos/orchestrator-agent": "*",
    "@talos/research-agent": "*",
    "@talos/execution-agent": "*",
    "@talos/recovery-agent": "*",
    "@talos/execution-monitor": "*",
    "fastify": "^5.2.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/websocket": "^11.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "vitest": "^3.0.0"
  }
}
